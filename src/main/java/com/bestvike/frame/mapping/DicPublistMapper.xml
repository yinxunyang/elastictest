<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bestvike.frame.dao.DicPublistDao">
    <select id="getAllPublist" parameterType="Map" resultType="DicPublist">
        <![CDATA[
			select itemName,value from Dic_Publist where dicName = 'certType'
		]]>
    </select>
    <select id="getCertType" parameterType="Map" resultType="DicPublist">
        <![CDATA[
			select itemName,value from Dic_Publist where dicName = 'certType'
		]]>
    </select>
</mapper>
