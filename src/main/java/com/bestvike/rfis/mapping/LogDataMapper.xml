<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bestvike.rfis.dao.LogDataDao">
	<insert id="addLog">
			insert into logdata(logId,beginTime, endTime, numbers, scopeBeginTime, scopeEndTime)
			 values (#{logId},#{beginTime}, #{endTime}, #{numbers}, #{scopeBeginTime} ,#{scopeEndTime})
    </insert>
</mapper>